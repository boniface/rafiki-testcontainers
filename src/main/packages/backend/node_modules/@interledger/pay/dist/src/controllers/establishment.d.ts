import { RequestState, StreamController } from '@interledger/pay/dist/src/controllers';
import { StreamReply, RequestBuilder } from '@interledger/pay/dist/src/request';
import { PaymentDestination } from '@interledger/pay/dist/src/open-payments';
export declare class EstablishmentController implements StreamController {
    private readonly destinationAddress;
    private isConnected;
    constructor({ destinationAddress }: PaymentDestination);
    didConnect(): boolean;
    buildRequest(request: RequestBuilder): RequestState;
    applyRequest(): (reply: StreamReply) => void;
}
