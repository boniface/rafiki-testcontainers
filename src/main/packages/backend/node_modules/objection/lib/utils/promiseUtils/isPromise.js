'use strict';

const { isObject, isFunction } = require('objection/lib/utils/objectUtils');

function isPromise(obj) {
  return isObject(obj) && isFunction(obj.then);
}

module.exports = {
  isPromise,
};
