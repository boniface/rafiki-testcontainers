'use strict';

const { WhereInEagerOperation } = require('objection/lib/queryBuilder/operations/eager/WhereInEagerOperation');

class NaiveEagerOperation extends WhereInEagerOperation {
  batchSize() {
    return 1;
  }
}

module.exports = {
  NaiveEagerOperation,
};
