import { SendState, StreamController } from '@interledger/pay/dist/src/controllers';
import { Plugin, RequestBuilder } from '@interledger/pay/dist/src/request';
import { StreamSender } from '@interledger/pay/dist/src/senders';
import { ResolvedPayment } from '@interledger/pay';
export declare class ConnectionCloser extends StreamSender<void> {
    private sentCloseFrame;
    protected readonly controllers: StreamController[];
    constructor(plugin: Plugin, destination: ResolvedPayment);
    nextState(request: RequestBuilder): SendState<void>;
}
