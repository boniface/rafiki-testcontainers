'use strict';

const { isPromise } = require('objection/lib/utils/promiseUtils/isPromise');
const { after } = require('objection/lib/utils/promiseUtils/after');
const { afterReturn } = require('objection/lib/utils/promiseUtils/afterReturn');
const { mapAfterAllReturn } = require('objection/lib/utils/promiseUtils/mapAfterAllReturn');
const { promiseMap } = require('objection/lib/utils/promiseUtils/map');
const { promiseTry } = require('objection/lib/utils/promiseUtils/try');

module.exports = {
  isPromise,
  after,
  afterReturn,
  mapAfterAllReturn,
  map: promiseMap,
  try: promiseTry,
};
