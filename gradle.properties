# Coordinates
GROUP=zm.hashcode
VERSION_NAME=0.1.0

# Central Portal credentials (token user/password from central.sonatype.com)
# Use env: ORG_GRADLE_PROJECT_mavenCentralUsername / mavenCentralPassword
mavenCentralUsername=
mavenCentralPassword=

# PGP signing (ASCII-armored key). Provide via env in CI.
signingKey=
signingPassword=

# For reproducible builds
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
