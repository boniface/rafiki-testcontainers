/// <reference types="node" />
import { Frame } from 'ilp-protocol-stream/dist/src/packet';
import Long from 'long';
import { IlpAddress, IlpFulfill, IlpReject, IlpErrorCode } from 'ilp-packet';
import { LongValue } from 'ilp-protocol-stream/dist/src/util/long';
export declare const sha256: (preimage: Buffer) => Buffer;
export declare const hmac: (key: Buffer, message: Buffer) => Buffer;
export declare const encrypt: (key: Buffer, plaintext: Buffer) => Buffer;
export declare const decrypt: (key: Buffer, ciphertext: Buffer) => Buffer;
export declare const base64url: (buffer: Buffer) => string;
export declare class ReplyBuilder {
    private encryptionKey?;
    private sequence;
    private receivedAmount;
    private frames;
    private ilpAddress;
    setIlpAddress(ilpAddress: IlpAddress): this;
    setEncryptionKey(key: Buffer): this;
    setSequence(sequence: Long): this;
    setReceivedAmount(receivedAmount: LongValue): this;
    addFrames(...frames: Frame[]): this;
    private buildData;
    buildFulfill(fulfillment: Buffer): IlpFulfill;
    buildReject(code: IlpErrorCode): IlpReject;
}
