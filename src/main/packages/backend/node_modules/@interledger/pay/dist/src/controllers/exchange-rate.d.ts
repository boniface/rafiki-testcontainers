import { StreamController } from '@interledger/pay/dist/src/controllers';
import { StreamReply, StreamRequest } from '@interledger/pay/dist/src/request';
import { Int, PositiveInt, PositiveRatio, Ratio } from '@interledger/pay/dist/src/utils';
export declare class ExchangeRateController implements StreamController {
    private lowerBoundRate;
    private upperBoundRate;
    constructor(lowerBoundRate?: Ratio, upperBoundRate?: PositiveRatio);
    applyRequest({ sourceAmount, log }: StreamRequest): (reply: StreamReply) => void;
    getLowerBoundRate(): Ratio;
    getUpperBoundRate(): PositiveRatio;
    estimateDestinationAmount(sourceAmount: Int): [Int, Int];
    estimateSourceAmount(destinationAmount: PositiveInt): [PositiveInt, PositiveInt] | undefined;
}
