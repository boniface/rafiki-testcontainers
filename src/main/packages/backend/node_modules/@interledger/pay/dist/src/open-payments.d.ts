/// <reference types="node" />
import { PaymentError, SetupOptions } from '@interledger/pay';
import { AssetDetails } from '@interledger/pay/dist/src/controllers/asset-details';
import { IlpAddress } from 'ilp-packet';
export interface PaymentDestination {
    sharedSecret: Buffer;
    destinationAddress: IlpAddress;
    destinationAsset?: AssetDetails;
    destinationPaymentDetails?: IncomingPayment;
    accountUrl?: string;
    destinationAccount?: string;
}
export interface Account {
    id: string;
    publicName: string;
    assetCode: string;
    assetScale: number;
    authServer: string;
}
export interface IncomingPayment {
    id: string;
    paymentPointer: string;
    completed: boolean;
    expiresAt?: number;
    description?: string;
    externalRef?: string;
    incomingAmount?: Amount;
    receivedAmount: Amount;
}
export interface Amount {
    value: bigint;
    assetCode: string;
    assetScale: number;
}
export declare const fetchPaymentDetails: (options: Partial<SetupOptions>) => Promise<PaymentDestination | PaymentError>;
export declare const queryAccount: (destinationAccount: string) => Promise<Account | PaymentDestination | PaymentError>;
