import transformComponentsObject from "openapi-typescript/dist/transform/components-object.js";
import transformPathsObject from "openapi-typescript/dist/transform/paths-object.js";
import transformSchemaObjectMap from "openapi-typescript/dist/transform/schema-object-map.js";
import transformWebhooksObject from "openapi-typescript/dist/transform/webhooks-object.js";
export function transformSchema(schema, ctx) {
    if (!schema)
        return {};
    const output = {};
    if (schema.paths)
        output.paths = transformPathsObject(schema.paths, ctx);
    else
        output.paths = "";
    if (schema.webhooks)
        output.webhooks = transformWebhooksObject(schema.webhooks, ctx);
    else
        output.webhooks = "";
    if (schema.components)
        output.components = transformComponentsObject(schema.components, ctx);
    else
        output.components = "";
    if (schema.$defs)
        output.$defs = transformSchemaObjectMap(schema.$defs, { path: "$defs/", ctx });
    else
        output.$defs = "";
    return output;
}
//# sourceMappingURL=index.js.map