'use strict';

const { GraphOperation } = require('objection/lib/queryBuilder/graph/GraphOperation');
const { GraphPatchAction } = require('objection/lib/queryBuilder/graph/patch/GraphPatchAction');

class GraphPatch extends GraphOperation {
  createActions() {
    return [
      new GraphPatchAction(this.graphData, {
        nodes: this.graph.nodes.filter((node) =>
          this.graphOptions.shouldPatchOrUpdateIgnoreDisable(node, this.graphData),
        ),
      }),
    ];
  }
}

module.exports = {
  GraphPatch,
};
