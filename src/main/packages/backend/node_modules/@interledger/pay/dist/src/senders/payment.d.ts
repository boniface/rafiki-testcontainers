import { SendState, StreamController } from '@interledger/pay/dist/src/controllers';
import { IntQuote, PaymentProgress, PayOptions } from '@interledger/pay';
import { RequestBuilder } from '@interledger/pay/dist/src/request';
import { StreamSender } from '@interledger/pay/dist/src/senders';
export declare enum PaymentType {
    FixedSend = "FixedSend",
    FixedDelivery = "FixedDelivery"
}
declare type PaymentSenderOptions = Omit<PayOptions, 'quote'> & {
    quote: IntQuote;
};
export declare class PaymentSender extends StreamSender<PaymentProgress> {
    static DEFAULT_STREAM_ID: number;
    private amountSent;
    private amountDelivered;
    private sourceAmountInFlight;
    private destinationAmountInFlight;
    private appliedRoundingCorrection;
    private remoteReceiveMax?;
    private latestReceipt?;
    private readonly quote;
    private readonly progressHandler?;
    protected readonly controllers: StreamController[];
    private readonly rateCalculator;
    private readonly maxPacketController;
    constructor({ plugin, destination, quote, progressHandler }: PaymentSenderOptions);
    nextState(request: RequestBuilder): SendState<PaymentProgress>;
    getProgress(): PaymentProgress;
    private updateReceiveMax;
    private updateStreamReceipt;
}
export {};
