import { QuoteOptions } from '@interledger/pay';
import { SendState, StreamController } from '@interledger/pay/dist/src/controllers';
import { PositiveInt, PositiveRatio, Ratio } from '@interledger/pay/dist/src/utils';
import { RequestBuilder } from '@interledger/pay/dist/src/request';
import { StreamSender } from '@interledger/pay/dist/src/senders';
export interface ProbeResult {
    maxPacketAmount: PositiveInt;
    lowEstimatedExchangeRate: Ratio;
    highEstimatedExchangeRate: PositiveRatio;
}
export declare class RateProbe extends StreamSender<ProbeResult> {
    private static TIMEOUT;
    static MAX_PROBE_AMOUNT: PositiveInt;
    private readonly remainingTestAmounts;
    private readonly inFlightAmounts;
    private deadline?;
    protected readonly controllers: StreamController[];
    private maxPacketController;
    private rateCalculator;
    constructor({ plugin, destination }: QuoteOptions);
    nextState(request: RequestBuilder): SendState<ProbeResult>;
}
