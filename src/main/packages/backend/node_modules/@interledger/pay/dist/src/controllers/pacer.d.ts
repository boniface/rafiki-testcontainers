import { RequestState, StreamController } from '@interledger/pay/dist/src/controllers';
import { StreamReply } from '@interledger/pay/dist/src/request';
export declare class PacingController implements StreamController {
    private static DEFAULT_PACKETS_PER_SECOND;
    private static MIN_PACKETS_PER_SECOND;
    private static MAX_PACKETS_PER_SECOND;
    private static PACKETS_PER_SECOND_INCREASE_TERM;
    private static PACKETS_PER_SECOND_DECREASE_FACTOR;
    private static DEFAULT_ROUND_TRIP_TIME_MS;
    private static ROUND_TRIP_AVERAGE_WEIGHT;
    private static MAX_INFLIGHT_PACKETS;
    private lastPacketSentTime;
    private averageRoundTrip;
    private packetsPerSecond;
    private inFlightCount;
    getPacketFrequency(): number;
    getNextPacketSendTime(): number;
    buildRequest(): RequestState;
    applyRequest(): (reply: StreamReply) => void;
}
