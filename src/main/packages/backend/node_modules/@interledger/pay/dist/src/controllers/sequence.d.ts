import { RequestState, StreamController } from '@interledger/pay/dist/src/controllers';
import { RequestBuilder } from '@interledger/pay/dist/src/request';
import { NonNegativeInteger } from '@interledger/pay/dist/src/utils';
export declare class Counter {
    private count;
    private constructor();
    static from(count: number): Counter | undefined;
    increment(): void;
    getCount(): NonNegativeInteger;
}
export declare class SequenceController implements StreamController {
    private readonly counter;
    private static PACKET_LIMIT;
    constructor(counter: Counter);
    buildRequest(request: RequestBuilder): RequestState;
    applyRequest(): undefined;
}
