import { StreamController } from '@interledger/pay/dist/src/controllers';
import { PaymentError } from '@interledger/pay';
import { PaymentDestination } from '@interledger/pay/dist/src/open-payments';
import { StreamReply } from '@interledger/pay/dist/src/request';
export declare const isValidAssetDetails: (o: any) => o is AssetDetails;
export declare const isValidAssetScale: (o: unknown) => o is number;
export interface AssetDetails {
    scale: number;
    code: string;
}
export declare class AssetDetailsController implements StreamController {
    private destinationAsset?;
    constructor({ destinationAsset }: PaymentDestination);
    getDestinationAsset(): AssetDetails | undefined;
    applyRequest(): (reply: StreamReply) => PaymentError | void;
}
