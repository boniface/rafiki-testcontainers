import { RequestState, StreamController } from '@interledger/pay/dist/src/controllers';
import { StreamReply, StreamRequest } from '@interledger/pay/dist/src/request';
export declare class TimeoutController implements StreamController {
    private static MAX_DURATION_SINCE_LAST_FULFILL;
    private deadline?;
    buildRequest(request: StreamRequest): RequestState;
    applyRequest(): (reply: StreamReply) => void;
    private resetDeadline;
}
