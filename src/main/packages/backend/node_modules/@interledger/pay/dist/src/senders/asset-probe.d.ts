import { SendState, StreamController } from '@interledger/pay/dist/src/controllers';
import { AssetDetails } from '@interledger/pay/dist/src/controllers/asset-details';
import { Counter } from '@interledger/pay/dist/src/controllers/sequence';
import { Plugin, RequestBuilder } from '@interledger/pay/dist/src/request';
import { StreamSender } from '@interledger/pay/dist/src/senders';
import { PaymentDestination } from '@interledger/pay/dist/src/open-payments';
export declare class AssetProbe extends StreamSender<AssetDetails> {
    private requestCount;
    private replyCount;
    private readonly establishmentController;
    private readonly assetController;
    protected readonly controllers: StreamController[];
    constructor(plugin: Plugin, destination: PaymentDestination, counter: Counter);
    nextState(request: RequestBuilder): SendState<AssetDetails>;
}
