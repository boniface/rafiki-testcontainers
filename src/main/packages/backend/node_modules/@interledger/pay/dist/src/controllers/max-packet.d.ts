import { StreamController } from '@interledger/pay/dist/src/controllers';
import { PositiveInt } from '@interledger/pay/dist/src/utils';
import { PaymentError } from '@interledger/pay';
import { StreamReply, StreamRequest } from '@interledger/pay/dist/src/request';
export declare class MaxPacketAmountController implements StreamController {
    private state;
    private verifiedPathCapacity;
    constructor(preciseMaxPacketAmount?: PositiveInt);
    getNextMaxPacketAmount(): PositiveInt | undefined;
    isProbeComplete(): boolean;
    getMaxPacketAmountLimit(): PositiveInt;
    applyRequest({ sourceAmount }: StreamRequest): (reply: StreamReply) => PaymentError | void;
    private reduceMaxPacketAmount;
    private adjustPathCapacity;
}
